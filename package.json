{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "preinstall": "rm -f package-lock.json yarn.lock",
    "prebuild": "npx pnpm@9.15.4 install --frozen-lockfile || npx pnpm@9.15.4 install --force",
    "build": "npx pnpm@9.15.4 run typecheck && npx pnpm@9.15.4 -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "npx pnpm@9.15.4 run typecheck:libs && npx pnpm@9.15.4 -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.3"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
